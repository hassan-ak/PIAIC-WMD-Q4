def add_two_numbers(a: int, b: int) -> int:
    """
    Adds two numbers together and returns the result.

    Args:
        a (int): The first number to add.
        b (int): The second number to add.

    Returns:
        int: The sum of the two numbers.
    """

    # Add the two numbers and return the result.
    # This is a simple function that adds two numbers together and returns the result.
    # It takes two integer arguments, a and b, and returns an integer.

    # Test cases:
    # add_two_numbers(1, 2) -> 3
    # add_two_numbers(5, 3) -> 8
    # add_two_numbers(-2, 7) -> 5

    # The function takes two integer arguments, a and b, and returns an integer.
    # Inside the function, it adds the two numbers together using the + operator and returns the result.

    # The test cases show calling the function with different integer arguments and printing the result.
    # This demonstrates how to write a simple function in Python to add two numbers together."
    return a + b
