def login(username, password):
    """
    This function checks if the given username and password are valid.
    If the username and password are valid, it returns True.
    If the username or password is invalid, it returns False.
    """

    # Check if the username and password are valid
    # Replace this with your own logic to validate the username and password

    # Example: Check if the username and password are 'admin' and 'admin' respectively

    # Return True if the username and password are valid, False otherwise

    # Test cases
    # Valid username and password: 'XXXXX' and 'admin'
    # Invalid username: 'XXXX'
    # Invalid password: 'XXXXXXXX'

    # Add your own test cases as needed

    # Example implementation:
    if username == "admin" and password == "admin":
        return True
    else:
        return False

    # Add your own test cases as needed

    # Example implementation:
    if username == "admin" and password == "admin":
        return True
    else:
        return False
